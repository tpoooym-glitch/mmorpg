function line(ctx,points,width,fill,stroke){if(!points||points.length<2)return;ctx.save();ctx.lineCap='round';ctx.lineJoin='round';ctx.lineWidth=width;ctx.strokeStyle=stroke;ctx.beginPath();for(let i=0;i<points.length;i++){const p=points[i];if(i)ctx.lineTo(p[0],p[1]);else ctx.moveTo(p[0],p[1])}ctx.stroke();ctx.lineWidth=Math.max(1,width-8);ctx.strokeStyle=fill;ctx.beginPath();for(let i=0;i<points.length;i++){const p=points[i];if(i)ctx.lineTo(p[0],p[1]);else ctx.moveTo(p[0],p[1])}ctx.stroke();ctx.restore()}
function drawBridge(ctx,b,cam){const x=b.x-cam.x,y=b.y-cam.y;ctx.save();ctx.imageSmoothingEnabled=false;ctx.fillStyle='#6f4b31';ctx.fillRect(x,y,b.w,b.h);const plank=Math.max(14,Math.round(b.w/8));for(let px=x;px<x+b.w;px+=plank){ctx.fillStyle='#a36d47';ctx.fillRect(px+1,y+3,plank-3,b.h-6)}ctx.strokeStyle='#3d291e';ctx.lineWidth=4;ctx.strokeRect(x,y,b.w,b.h);ctx.restore()}
export function drawRoadOverlay(ctx,state){const z=state.zone;if(!z)return;for(const r of z.roads||[]){const points=r.points.map(p=>[p[0]-state.cam.x,p[1]-state.cam.y]);const w=r.width||96;line(ctx,points,w,'#a87449','#6d482f')}for(const b of z.bridges||[])drawBridge(ctx,b,state.cam)}
