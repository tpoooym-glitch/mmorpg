export function updateHud(state){const p=state.player;const set=(id,value)=>{const el=document.getElementById(id);if(el)el.textContent=value};set('hp',Math.ceil(p.hp)+'/'+p.maxHp);set('mp',Math.ceil(p.mp)+'/'+p.maxMp);set('gold',p.gold);set('xp',p.xp+'/100');const hp=document.getElementById('hpbar'),mp=document.getElementById('mpbar');if(hp)hp.style.width=(100*p.hp/p.maxHp)+'%';if(mp)mp.style.width=(100*p.mp/p.maxMp)+'%';const character=document.getElementById('character');if(character)character.textContent=p.name+' · '+p.class+' · Lv.'+p.level}