import {dist} from '../core/utils.js';
const INTERACT_RANGE=70;
function nearest(state){let best=null,bestD=Infinity;for(const node of state.resources||[]){if(node.collected)continue;const d=dist(node.x,node.y,state.player.x,state.player.y);if(d<bestD){best=node;bestD=d}}return best&&bestD<=INTERACT_RANGE?{node:best,distance:bestD}:null}
function addItem(player,id,qty=1){player.inventory=player.inventory&&typeof player.inventory==='object'?player.inventory:{};player.inventory[id]=(player.inventory[id]||0)+qty}
export function createResourceInteraction(state){const source=Array.isArray(state.zone?.resources)?state.zone.resources:[];state.resources=source.map(n=>({...n,collected:false}));if(!state.resources.some(n=>n.type==='rare-herb'))state.resources.push({id:'rare-herb-1',type:'rare-herb',x:1150,y:2100,collected:false});return{current:null,update(){this.current=nearest(state);state.resourceInteraction=this.current;return this.current},interact(){const near=this.update();if(!near)return false;const node=near.node;node.collected=true;addItem(state.player,node.type,1);state.questManager?.progress('collect',node.type,1);state.ui={...(state.ui||{}),message:`เก็บ ${node.type} +1`};return true}}}
