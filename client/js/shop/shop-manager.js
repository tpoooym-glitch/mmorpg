export function createShopManager(state,items){return{items,buy(id,quantity=1){const item=this.items.find(x=>x.id===id);const cost=(item?.buyPrice??0)*quantity;if(!item||state.player.gold<cost)return false;state.player.gold-=cost;return true},sell(id,quantity=1){const item=this.items.find(x=>x.id===id);if(!item)return false;state.player.gold+=(item.sellPrice??0)*quantity;return true}}}
